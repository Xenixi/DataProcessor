#include <iostream>
#include <string>

int main(int argc, char **argv)
{
    std::cout << "DPR 1.0: Enter file(s) separated by commas: ";
    std::string input; 

    std::getline(std::cin, input);

    std::cout << "Attempting to load '" << input << "'" << std::endl;

    

}